name: Build & Test

on: push

jobs:

    build:
        name: Build & Test
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v6
            - uses: actions/setup-node@v6
              with:
                  node-version: latest
            - run: npm ci
            - run: npm run build
